# A4 PDF出力ツール

A4サイズのPDFを生成するHTML&JSアプリケーション

## セットアップ

### 依存関係のインストール

```bash
pip install -r requirements.txt
```

## 使い方

### 開発サーバーの起動

```bash
python dev_server.py
```

ブラウザで `http://localhost:8000` にアクセスしてください。

ファイルを変更すると自動的にブラウザがリロードされます。

### 通常の使用

`index.html` をブラウザで直接開いても動作します。